
<body class="bg-gray-100 min-h-screen flex items-center justify-center p-6">

  <div class="bg-white shadow-md rounded-lg p-8 max-w-xl w-full mt-16 mx-auto">
    <h1 class="text-2xl font-bold text-gray-700 mb-6">✏️ Editar tarea #<?= $this->task->idTask ?></h1>

    <form method="post" action="<?= BASE_URL ?>/taskUpdate?id=<?= $this->task->idTask ?>" class="space-y-5">

      <!-- Descripción -->
      <div>
        <label class="block text-sm font-medium text-gray-700">Descripción</label>
        <input type="text" name="descriptionTask"
               value="<?= htmlspecialchars($this->task->descriptionTask) ?>" required
               class="mt-1 block w-full rounded-md border border-gray-300 px-3 py-2 shadow-sm focus:ring-blue-500 focus:border-blue-500">
      </div>

      <!-- Usuario -->
      <div>
        <label class="block text-sm font-medium text-gray-700">Usuario</label>
        <input type="text" name="userTask"
               value="<?= htmlspecialchars($this->task->userTask) ?>" required
               class="mt-1 block w-full rounded-md border border-gray-300 px-3 py-2 shadow-sm focus:ring-blue-500 focus:border-blue-500">
      </div>

      <!-- Tipo de tarea -->
      <div>
        <label class="block text-sm font-medium text-gray-700">Tipo de tarea</label>
        <select name="taskTipe"
                class="mt-1 block w-full rounded-md border border-gray-300 px-3 py-2 bg-white shadow-sm focus:ring-blue-500 focus:border-blue-500">
          <?php foreach (TaskTipe::cases() as $tipo): ?>
            <option value="<?= $tipo->value ?>" <?= $tipo === $this->task->taskTipe ? 'selected' : '' ?>>
              <?= ucfirst($tipo->value) ?>
            </option>
          <?php endforeach; ?>
        </select>
      </div>

      <!-- Fecha -->
      <div>
        <label class="block text-sm font-medium text-gray-700">Fecha</label>
        <input type="date" name="dateTask"
               value="<?= $this->task->dateTask->format('Y-m-d') ?>" required
               class="mt-1 block w-full rounded-md border border-gray-300 px-3 py-2 shadow-sm focus:ring-blue-500 focus:border-blue-500">
      </div>

      <!-- Estado -->
      <div>
        <label class="block text-sm font-medium text-gray-700">Estado</label>
        <select name="taskStatus"
                class="mt-1 block w-full rounded-md border border-gray-300 px-3 py-2 bg-white shadow-sm focus:ring-blue-500 focus:border-blue-500">
          <?php foreach (TaskStatus::cases() as $estado): ?>
            <option value="<?= $estado->value ?>" <?= $estado === $this->task->taskStatus ? 'selected' : '' ?>>
              <?= ucfirst($estado->value) ?>
            </option>
          <?php endforeach; ?>
        </select>
      </div>

      <!-- Botones -->
      <div class="flex justify-between items-center pt-4">
        <a href="<?= BASE_URL ?>/userView"
           class="text-sm text-gray-600 hover:underline">⬅ Cancelar</a>
        <button type="submit"
                class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-md shadow">
          💾 Guardar cambios
        </button>
      </div>

    </form>
  </div>

</body>

